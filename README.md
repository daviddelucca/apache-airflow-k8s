Sample files to deploy Apache Airflow into your K8s Cluster.

Update <change> values to real values.

TODO:

Update configmap to read values from environment variables - I'll update this feat on next days.